# Shizen Power – Site estático (logos locais)
- Todas as bandeiras estão em `assets/payments/` (sem links externos).
- Para substituir por imagens oficiais, basta trocar os arquivos mantendo os nomes.
- Publicação: GitHub (upload dos arquivos) + Netlify (Import from Git, Publish dir `/`).